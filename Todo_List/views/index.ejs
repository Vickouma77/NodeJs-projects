<!--
   index.ejs is the main page of the application.
-->

<!DOCTYPE html>
<html>
<head>
    <title>Todo List</title>
</head>
<body>
    <h2>Todo List</h2>
    <table>
        <tr>
            <th>Id</th>
            <th>Task</th>
            <th>Completed</th>
        </tr>
        <% data.forEach(element) { %>
            <tr>
                <td><%= element.id %></td>
                <td><%= element.task %></td>
                <td><%= element.completed %></td>
            </tr>
        <% } %>
    </table>
    <form action="/add" method="post">
        <input type="text" name="task" placeholder="Task">
        <input type="submit" value="Add">
</body>
</html>
